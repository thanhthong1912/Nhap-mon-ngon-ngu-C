#include <stdio.h>

int main(){
  int dongia, soluong, thanhtien;
  printf("don gia:", dongia);
  scanf("%d", &dongia);
  printf("so luong:\n", soluong);
  thanhtien = dongia * soluong;
  if(soluong>=5);
  printf("thanhtien = %d\n", thanhtien * 75/100);
  getchar();
  return 0;
}
